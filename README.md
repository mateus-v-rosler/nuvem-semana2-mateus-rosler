- foram usadas duas API geocoding e forecast, quando alguem clica no botão é enviada uma requisição para a geocoding quero retorna a latitude e longitude da cidade, e depois envia para a forecast que retorna as informações da cidade
